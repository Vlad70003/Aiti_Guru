import React from 'react';
import Logotype from 'assets/img/logo.svg';

const Logo = () => {
    return (
        <div>
            <img src={Logotype} alt='logo' />
        </div>
    );
};

export default Logo;