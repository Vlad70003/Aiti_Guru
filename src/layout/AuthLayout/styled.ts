import styled from "styled-components";
import {Flex} from "antd";

export const AuthPageWrapper = styled(Flex)`
    border-radius: 40px;
    background: white;
    padding: 6px;
    margin: auto auto;
`

export const AutoPageContainer = styled(Flex)`
    padding: 48px;
    background: linear-gradient(to bottom, #F9F9F9, #ffffff);
    border-radius: 34px;
`